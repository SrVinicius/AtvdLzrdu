import {createGlobalStyle } from 'styled-components'

const Corpo ={
    default: css`
        font-size: x-large;
        float: right;
        margin-right: 400px;
        margin-top: 180px;

    `
}

const LogoCorpo ={
    default: css`
        margin-top: 130px;
        margin-left: 400px;
        align-items: center;
    `
}